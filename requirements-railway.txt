# Railway Deployment - Lightweight Requirements
# Only essential packages for API functionality

# Web Framework and APIs  
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
httpx>=0.24.0

# Database and Storage
duckdb>=0.9.0
sqlalchemy>=2.0.0

# Data Processing (lightweight)
pandas>=2.0.0
numpy>=1.24.0,<2.0.0

# Data Collection
requests>=2.31.0
beautifulsoup4>=4.12.0

# Utilities
python-dotenv>=1.0.0
PyJWT>=2.8.0
geopy>=2.4.0

# Note: Heavy ML packages (torch, sentence-transformers, langchain) excluded for Railway
# These can be added later if needed, or use a different deployment strategy for ML features